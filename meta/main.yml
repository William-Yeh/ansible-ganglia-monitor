---
# meta/main.yml

galaxy_info:
    author: williamyeh
    description: Install ganglia-monitor (Ganglia Monitoring Daemon, gmond) and modules
    license: Apache
    min_ansible_version: 1.8

    platforms:

        - name: Ubuntu
          versions:
              - precise
              - trusty

        - name: Debian
          versions:
              - wheezy

    categories:
        - system
        - monitoring

dependencies: []